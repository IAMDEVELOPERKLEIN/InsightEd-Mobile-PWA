name: "🚀 Auto-Deploy to VM"

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "☁️ Triggering Remote Pull & Deploy"
        uses: appleboy/ssh-action@master
        with:
          host: "20.24.58.49"
          username: "Administrator1"
          password: "7v52E69TYgTE"
          script: |
            echo "------------------------------------------------"
            echo "🚀 Starting Remote Auto-Deploy..."
            echo "------------------------------------------------"
            cd /var/www/html/InsightEd-Mobile-PWA
            
            echo "📥 1. Pulling latest code from GitHub..."
            git pull origin main
            
            echo "🏗️ 2. Running local production script..."
            chmod +x deploy.sh
            ./deploy.sh
            
            echo "------------------------------------------------"
            echo "✅ Deployment Successful!"
            echo "------------------------------------------------"
