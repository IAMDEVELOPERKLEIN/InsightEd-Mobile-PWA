const BarongMascot = ({ className }) => {
    return (
        <div className={`${className || "w-16 h-16"} flex items-center justify-center text-4xl leading-none select-none`}>
            🦁
        </div>
    );
};

export default BarongMascot;
