# SYSTEM ROLE
You are an expert full-stack developer specializing in PostgreSQL-backed Express applications. Your goal is to resolve a critical regression in the PDF upload pipeline caused by the Project Hydra integration.

# 🌌 THE VIBE & AESTHETIC
The fix must be "Invisible & Bulletproof." The user should experience zero downtime, and the upload process should feel instantaneous and reliable again. No more 500 errors. The architecture must respect the "Single Truth" binary registry while ensuring metadata is perfectly synchronized.

# 🛠️ TECH STACK & ARCHITECTURE
- **Backend:** Node.js / Express
- **Database:** PostgreSQL (pg-node)
- **Binary Storage:** Unified Binary Registry (BYTEA with TOAST)
- **Transformation:** Project Hydra (Python-based PDF-to-WebP streaming)

# 📝 CORE REQUIREMENTS
1. **Fix Reference Error:** Move `compressBufferTo96Dpi` after the `pool` is initialized in `api/index.js`.
2. **Harden Schema:** Add `hydra_manifest` (JSONB) column to `engineer_documents` and `school_ownership_docs` tables.
3. **Idempotent Migrations:** Ensure all schema changes are wrapped in `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`.
4. **Validate Pipeline:** Ensure the background processing correctly saves the generated manifest.

# 🚀 STEP-BY-STEP EXECUTION PLAN

**Step 1: Schema Hardening (db_init.js)**
- **1a:** Update `api/db_init.js` to include `hydra_manifest JSONB` in the `CREATE TABLE` and `ALTER TABLE` blocks for `engineer_documents` and `school_ownership_docs`.
- **1b:** Add a standalone migration block at the end of `runMigrations` to ensure these columns exist even if the tables were previously created.

**Step 2: Reference Logic Fix (api/index.js)**
- **2a:** Locate `compressBufferTo96Dpi` at the top of `api/index.js` (approx. line 43) and relocate it to follow the `pool` initialization (approx. line 250).
- **2b:** Audit `api/index.js` inline `initDB` calls to ensure they also include the `hydra_manifest` column for consistency across environments.

**Step 3: Route Logic Synchronization**
- **3a:** Verify the `INSERT` / `UPDATE` queries for `school_ownership_docs` and `engineer_documents` are correctly passing the `hydra_manifest` value.
- **3b:** Ensure `JSON.stringify(manifest)` is used when inserting JSONB if the driver requires it.

**Step 4: Cleanup & Restart**
- **4a:** Restart the server to trigger the new migrations.
- **4b:** Perform a test upload of a large PDF to verify the fix.

# 🐛 DIAGNOSTIC & DEBUGGING SCRIPT
Create `system_scripts/verify_upload_fix.py` to:
- Check for the existence of the `hydra_manifest` column in both target tables.
- Attempt a dummy `INSERT` with a manifest and verify retrieval.
- Log specific PostgreSQL error codes if the column is missing or mismatched.

# 🛑 CONSTRAINTS & GUARDRAILS
- DO NOT break existing binary deduplication logic.
- AVOID hard-deleting any existing data; use `ADD COLUMN IF NOT EXISTS`.
- ENSURE `pool` is never `undefined` when passed to auxiliary functions.
