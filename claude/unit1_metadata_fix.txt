# SYSTEM ROLE
You are an expert full-stack developer operating in a Node.js (ESM), Express, and PostgreSQL environment. Your goal is to write clean, modular, and highly performant code to resolve document metadata recording issues in the InsightEd platform.

# 🌌 THE VIBE & AESTHETIC
This needs to feel like a high-end FinTech or GovTech application—bulletproof, transparent, and lightning-fast. The user should feel that their data, especially binary assets like PDFs, is handled with absolute cryptographic and logical integrity.

# 🛠️ TECH STACK & ARCHITECTURE
- **Frontend:** React (Modular Components)
- **Backend:** Node.js 20+ (ESM), Express.js
- **Database:** PostgreSQL (with Unified Binary Registry)
- **Key Patterns:** Ghost Developer / Hawkeye Protocol (High-autonomy, deterministic state)

# 📝 CORE REQUIREMENTS
1. **Fix Backend Scope Leak:** The variable `storedSize` in `api/index.js` must be properly scoped to be accessible by the database insertion logic.
2. **Implement Defensive Coding:** Add robust null checks for `req.file` to prevent server runtime exceptions.
3. **Enhance API Response:** Return `file_size` and `file_name` in the successful upload response to ensure frontend state hydration.
4. **Synchronize Frontend State:** Initialize `local_file_size` in the Unit 1 form state and ensure it's updated on successful upload.

# 🚀 STEP-BY-STEP EXECUTION PLAN

**Step 1: Backend Variable Scope & Safety (api/index.js)**
- **1a:** Identify the `POST /api/schools/:iern/ownership-docs` route.
- **1b:** Move the declaration of `storedSize` outside the inner `try` block to ensure visibility at the SQL query level.
- **1c:** Inject a sentinel check: `if (!req.file) return res.status(400).json({ error: "No file provided" });`.
- **1d:** Map the `stored_size` from the binary pipeline back to `storedSize`.

**Step 2: API Payload Refinement**
- **2a:** Update the success JSON object to include `file_size` and `fileName`.
- **2b:** Verify the `id` and `filePath` are correctly returned.

**Step 3: Frontend State Hydration (Unit1SchoolIdentity.jsx)**
- **3a:** Update the `formData` initial state to include `local_file_size: null`.
- **3b:** Ensure the `onUploadSuccess` callback in the `DocumentUpload` component correctly maps the 4th parameter (`size`) to `local_file_size`.

**Step 4: Persistence Alignment**
- **4a:** Ensure `local_file_size` is passed in the `dataToSend` object during the final form submission (Unit 1 POST).

# 🐛 DIAGNOSTIC & DEBUGGING SCRIPT
Provide a lightweight diagnostic script `tmp_verify_metadata.cjs` that:
- Simulates a multipart/form-data upload to the local endpoint.
- Verifies that the JSON response contains a non-null `id`, `filePath`, and `file_size`.
- Includes a simple toggle `const DEBUG_VERIFY = true;` to log full response bodies.

# 🛑 CONSTRAINTS & GUARDRAILS
- **DO NOT** create `ph_schools` records automatically.
- **DO NOT** use `require`; use `import` (ESM mode).
- **AVOID** generic error messages like "Server Error"; use the context-aware error handling already present in the Hawkeye protocol.
